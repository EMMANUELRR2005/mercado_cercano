# Copia este archivo a android/secrets.properties (NO se versiona) y pon tu
# Google Maps API key para Android. Gradle la inyecta en el AndroidManifest
# como ${MAPS_API_KEY}.
#
# Restringe la key en Google Cloud Console por nombre de paquete + SHA-1.
MAPS_API_KEY=YOUR_ANDROID_MAPS_API_KEY
